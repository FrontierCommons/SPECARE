import { registerRootComponent } from 'expo';

import App from './App';

// Registers App as the root so the same entry point works unchanged in both
// Expo Go and a native build.
registerRootComponent(App);
